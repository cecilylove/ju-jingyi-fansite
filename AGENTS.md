# AGENTS.md · 橘光集 项目说明

> 本文档面向 **任何后续维护者（人或 AI Agent）**。
> 看完这一份，你就能知道站点是怎么组织的、内容存在哪里、怎么改。

---

## 一、这是什么

「橘光集」（Orange Light）——鞠婧祎的**非官方粉丝纪念站**。

- **站名**：橘光集 · Orange Light
- **技术**：纯静态 HTML + 手写 CSS + 原生 JS，**零依赖、零构建**。
- **部署**：EdgeOne Makers（绑定 GitHub 仓库 `cecilylove/ju-jingyi-fansite`，push 即自动部署）。
- **设计原则**：**布局定死，内容全部外置**。页面 HTML 里只有空容器，内容一律放在 `data/`。

### 站名出现在哪些地方

导航栏、页脚、版权行里的站名都由 `data/site.js` 渲染，**只需改那一个文件**。

但有两处写死在 HTML 里，改站名时要手动同步：

| 位置 | 说明 |
|---|---|
| 各页 `<title>` | 如 `作品 · 鞠婧祎 · 橘光集` |
| 各页 `<meta name="description">` | 以「橘光集 · 」开头 |

---

## 二、目录结构

```
ju-jingyi-fansite/
├── index.html            # 首页
├── about.html            # 关于她
├── works.html            # 作品
├── gallery.html          # 图库
├── news.html             # 动态
├── update.html           # 动态详情页（一篇通用，按 ?id= 取内容）
├── data/                 # ★ 内容数据（改内容只改这里）
│   ├── site.js           #   站点外壳：站名/导航/页脚/合规声明 ← 所有页面都引
│   ├── home.js           #   首页专属内容
│   ├── about.js          #   关于她
│   ├── works.js          #   作品
│   ├── gallery.js        #   图库
│   └── news.js           #   动态（含详情页正文）
├── assets/
│   ├── css/styles.css    # 全站样式
│   ├── js/main.js        # 全站交互 + 所有渲染器
│   └── img/              # 图片素材
└── AGENTS.md             # 本文件
```

---

## 三、核心机制（必读）

### 3.1 内容是怎么显示出来的

HTML 里只放**空容器**，带一个特殊属性；`main.js` 读 `data/*.js` 后把内容填进去。

```html
<div class="grid grid-3" data-render="dramas" data-source="WORKS"></div>
```

### 3.2 渲染属性速查

| 属性 | 作用 | 读哪里 |
|---|---|---|
| `data-site-nav` | 导航栏（整个 `<header>` 内容） | `SITE.nav` |
| `data-site-footer` | 页脚（整个 `<footer>` 内容） | `SITE.footerGroups` / `SITE.social` |
| `data-site-notice[="key"]` | 合规声明，默认 `default`；图库用 `image` | `SITE.notices` |
| `data-pagehead` | 页面大标题（kicker + h2 + p） | `SOURCE.pageHead` |
| `data-heading="键名"` | 区块标题（kicker + h2 + p） | `SOURCE.sections.键名` |
| `data-subtitle="键名"` | 小标题（纯文字 h3） | `SOURCE.subTitles.键名` |
| `data-render="键名"` | 通用卡片组 | `SOURCE.键名`（数组） |
| `data-render-gallery` | 图库九宫格 | `GALLERY.photos` |
| `data-render-about="键名"` | 关于她的卡片组 | `ABOUT.键名` |
| `data-render-milestones` | 成长历程时间线，可加 `data-limit="N"` 截取前 N 条 | `SITE.milestones` |
| `data-render-news="键名"` | 动态时间线 | `NEWS.键名` |
| `data-render-news-articles` | 动态卡片（自动生成详情页链接） | `NEWS.articles` |
| `data-home-hero="键名"` | 首页首屏文案/图片 | `HOME.hero` |
| `data-render-home="键名"` | 首页各区块（`milestones` 那条读的是 `SITE.milestones`，不是 HOME） | `HOME.键名` |
| `data-update-root` | 详情页正文整块 | `NEWS.articles`（按网址 `?id=`） |

> `data-source` 指定数据源（`SITE` / `HOME` / `ABOUT` / `WORKS` / `GALLERY` / `NEWS`），缺省为 `WORKS`。

### 3.3 ★ 脚本加载顺序（改错就白屏）

`data/*.js` 必须在 `assets/js/main.js` **之前**：

```html
<script src="data/site.js"></script>      <!-- 外壳，所有页面都要 -->
<script src="data/works.js"></script>     <!-- 本页专属 -->
<script src="assets/js/main.js"></script> <!-- 最后 -->
```

### 3.4 ★★ main.js 内部执行顺序（最容易被改坏的地方）

`main.js` 里各段是**按固定顺序**执行的，**不要随意调换**：

1. **站点外壳渲染**（导航/页脚/声明）—— 必须最先，因为第 2 段要绑到渲染出来的汉堡按钮上
2. 导航交互（滚动状态 + 移动端菜单）
3–9. 各页数据渲染
10. **滚动揭示动画 —— 必须最后**
11. 空容器兜底提示

**为什么第 10 段必须最后？**

CSS 里有 `.reveal-ready .reveal { opacity: 0 }`。滚动动画一旦初始化，就会给 `<html>` 加上 `reveal-ready`，
此后**所有**带 `.reveal` 的元素都会变透明，只有被 `IntersectionObserver` 观察到的才会淡入。

如果动画初始化早于数据渲染，JS 渲染出来的卡片不会被观察 → **永久透明，整页卡片看不见**。

> 这是 2026-09 数据化改造时踩过的坑（改造前卡片写死在 HTML 里，所以没暴露）。
> 自检脚本里有一条专门的回归断言盯这个问题，别删。

### 3.5 数据没显示出来怎么办

容器为空时，页面会自动插入一条红框提示「内容未加载」。排查顺序：

1. 页面底部有没有引对应的 `data/*.js`？（见 3.3）
2. 浏览器控制台有没有 `找不到 XXX.YYY，已跳过`？→ 键名写错了
3. 数据文件里是不是 `window.XXX = { ... }` 开头？写成 `const XXX = ...` 会找不到

### 3.6 ★★ 共用内容的「单一源头」规则（最容易犯的错）

**同一个内容出现在多个页面时，数据只能有一份，其他页面去「截取」。**

站上有两组这样的内容：

**① 成长里程碑 → 源头是 `data/site.js` 的 `SITE.milestones`（9 条）**

| 位置 | 取多少 | 定位 |
|---|---|---|
| 关于她 · 成长历程 | **全部 9 条** | 里程碑 —— 「她怎么走到今天」 |
| 首页 · 成长里程碑 | **前 4 条**（`data-limit="4"`） | 首页钩子，只挑最亮的 |

**② 近期动态 → 源头是 `data/news.js` 的 `NEWS.articles`**

| 位置 | 取多少 |
|---|---|
| 动态 · 近期动态卡片 | 全部 |
| 首页 · 近期动态卡 | **前 2 条**（`data-limit="2"`） |
| 动态详情页 `update.html` | 按 `?id=` 取单篇 |

> ⚠️ 首页为了读 `NEWS.articles`，`index.html` 里**也要引 `data/news.js`**（在 `main.js` 之前）。

**③ 星河纪事 → `NEWS.timeline`（37 条）—— ⚠️ 当前不在任何页面展示**

2026-09-17 站主决定把它从动态页移除。理由：**37 条历年记录放在「动态」栏目里，与栏目语义不符**
——「动态」意味着最近消息，而纪事讲的是 2013 年以来发生了什么。

**但数据保留**：它是站上最完整的一份事实档案，含公益、晚会、团体时期细节
（这些内容 `SITE.milestones` 刻意不收，丢了就没了）。
想恢复展示时**不用动数据**，在目标页面加一个容器即可：

```html
<div class="timeline" data-render-news="timeline"></div>
```

> `date` 格式：优先精确到日 `YYYY.MM.DD`，无精确日期时用 `YYYY.MM` 或 `YYYY`。
> ⚠️ **必须以 4 位年份开头**——自检脚本靠前 4 位做年份匹配（里程碑覆盖检查、合约表述检查）。

**里程碑的判定标准：只有「身份或阶段发生改变」的才算。**

- ✅ 算：出道、首次登顶、连霸+单飞、从偶像到演员被认可、主流奖项认可、登上央视春晚、自己主导专辑、合约到期独立
- ❌ 不算：**某部剧播出、某首歌发行、参加了某个节目**（这类「纪事」性质的内容可放 `NEWS.timeline`）

**为什么这条规则重要（都是真实踩过的坑）：**

- 首页里程碑与关于她成长历程曾**各自维护一份**，很快漂移：首页**漏掉了 2016 总决选冠军和 2019 福布斯**，2026 那条两处说法还不一致。
- 首页动态卡与动态页文章曾**各自维护一份**，标题写成了两个版本：
  首页「凸凸棉品牌全球代言人」／动态页「鞠婧祎成为凸凸棉品牌全球代言人」。

**加内容时的自查：这个内容是不是已经在别处有了？如果有，去源头加，不要在第二个文件里复制。**

> 两个自检脚本会盯着这两条不变量：
> `data-consistency-test.js` 检查 `HOME.milestones` / `HOME.updates` / `ABOUT.journey` **不存在**（防重复数据回流）；
> `e2e-render-test.js` 跨页面比对两处渲染出的文案是否**逐字一致**。

---

## 四、各数据文件详解

### 1. `data/site.js` —— 站点外壳（全站共享）

```js
window.SITE = {
  meta: {
    name: '橘光集', nameEn: 'Orange Light',
    tagline: '页脚标语',
    footerNote: '页脚第一行', copyright: '© 2026 橘光集 · 非官方粉丝站'
  },
  nav: [ { key: 'index', href: 'index.html', label: '首页' }, ... ],
  footerGroups: [ { title: '站点', links: [ { href, label } ] }, ... ],
  social: [ { label: '微博', href: '...' } ],
  milestones: [ { date: '2013', title: '出道', desc: '...' }, ... ],  // ★ 里程碑唯一源头
  notices: {
    default: { title: '非官方声明', text: '支持 HTML 标签' },
    image:   { title: '图片版权说明', text: '...' }
  }
};
```

> `nav[].key` 要和各页 `<body data-page="xxx">` 对应，用来点亮当前页。

### 2. `data/home.js` —— 首页专属

包含 `hero` / `heroActions` / `fandomIntro` / `fandom` / `sections`（4 个区块标题）/
`about`（关于她摘要）/ `workCards`（2 张作品卡）/ `galleryPreview`。

> ⚠️ 这里**没有** `milestones`，也**没有** `updates` —— 见 3.6：
> 首页的「成长里程碑」读 `SITE.milestones`，「近期动态」读 `NEWS.articles`。

### 3. `data/about.js` —— 关于她

`profile`（含 `tag` / `name` / `paragraphs` / `image`）、`fandom`(3)、`honors`(11)、`pageHead`、`sections`(3)。

> ⚠️「成长历程」**不在这里** —— 见 3.6，里程碑的唯一源头是 `data/site.js` 的 `SITE.milestones`。
> 本页用 `<div class="timeline" data-render-milestones></div>` 读它。

### 4. `data/works.js` —— 作品（内容最多）

| 键名 | 板块 | 数量 | 标签字段 |
|---|---|---|---|
| `albums` | 个人专辑与单曲 | 21 | `year` |
| `ost` | 影视 · 动画 · 游戏原声 | 18 | **`drama`（作品名）** |
| `films` | 电影 | 5 | `year` |
| `dramas` | 剧集 | 17 | `year` |
| `variety` | 综艺与晚会 | 12 | `year` |
| `upcoming` | 待播 · 拍摄中 | 2 | `year` |

另有 `pageHead`、`sections`(5)、`subTitles`（`albums` / `ost`）。

**加一部新剧**：在 `dramas` 里加一行 `{ year: '2027', title: '《新剧名》', desc: '饰 角色名。简介。' },`

> ⚠️ `ost` 用 `drama` 字段存**作品名**（不限于剧，也含动画 / 手游），而不是 `year`：
> `{ drama: '《剧名》', title: '歌曲名', desc: '说明' },`

**两条内容约定（2026-09 核实后确立）：**

1. **一人分饰多角时，角色名要全部写上**，例如
   《花戎》魏枝 / 司马忘月 / 魔后、《花间令》杨采薇 / 上官芷、《月鳞绮纪》露芜衣 / 地珠。
2. **翻唱要在 `desc` 里写明**。明确是他人原唱的（如《花》翻唱生物股长《SAKURA》、
   《壁上观》原唱为一棵小葱 / 张曦匀），必须标注「翻唱」，不要写成她的原唱作品。
   同理，OST 里要写清**歌曲定位**（主题曲 / 片头曲 / 片尾曲 / 插曲 / 推广曲 / 角色曲）——
   站上曾把《如意芳霏》的《梦渡》误写成「主题曲」，实为「片尾曲」。

### 5. `data/gallery.js` —— 图库

```js
window.GALLERY = {
  photos: [ { src: 'assets/img/gallery-01.jpg', label: '营业图 · 一' } ],
  pageHead: { kicker: 'Gallery', title: '营业图', desc: '...' }
};
```

**加图片三步**：图片放进 `assets/img/`（文件名用英文数字）→ 在 `photos` 末尾加一行 → 保存。

> ⚠️ 扩展名必须与真实格式一致。历史踩坑：JPEG 标成 `.png`、HEIC 存成 `.jpg` 都会裂图。

### 6. `data/news.js` —— 动态 + 详情页

- `timeline`(37)：星河纪事 —— **当前不在任何页面展示**（原因见 3.6），数据保留备查
- `articles`：动态文章，**含详情页正文**（`id` / `cover` / `summary` / `facts` / `source`）
- `brands`(10)：**品牌合作**（商务代言一览，`brand` / `title` / `date` / `href`）
  ⚠️ `title` 必须逐字照抄官方头衔 ——「全球品牌代言人」「品牌全球代言人」「品牌大使」
  「超级会员代言人」不是一回事，**别顺手统一成"代言人"**；`href` 必须是品牌官方账号的官宣帖
- `pageHead`、`sections`

> ⚠️ 首页的「近期动态卡」读的就是这里的 `articles` 前 2 条（同源），
> 所以加一条动态**只需要在这个文件加一次**，首页会自动跟上。

---

## 五、常见任务怎么做

### 加一条新动态（★ 现在只改一个文件）

1. 在 `data/news.js` 的 `articles` 数组**最前面**加一项：

   ```js
   {
     id: 'magazine-2027-01',            // 唯一，会出现在网址里
     date: '2027.01.15', tag: '杂志动态',
     title: '标题', desc: '一句话导语',
     cover: 'assets/img/gallery-03.jpg', coverAlt: '站内配图',
     coverCaption: '站内配图，非本次动态官方物料 · 图片来源：微博 @鞠婧祎',
     summary: '正文摘要……',
     facts: [ { label: '动态分类', value: '杂志动态' } ],
     source: { note: '本页信息依据官方公开微博整理。', href: 'https://...', label: '查看原文' }
   }
   ```

2. 保存 → `git push`

> **不用新建 HTML 文件，也不用改首页。** 详情页的「上一篇 / 下一篇」按数组顺序自动生成，
> 首页的「近期动态」会自动显示最前面 2 条（见 3.6）。

### 加一张新图 / 新剧 / 新电影 / 新歌

见上文「各数据文件详解」。

### 改导航、页脚、站名、合规声明

改 `data/site.js`，全站生效。

### 改区块标题（「音乐作品」那一行）

改对应数据文件的 `pageHead`（页面大标题）或 `sections`（区块标题）。

### 改样式

改 `assets/css/styles.css`。设计变量在文件顶部 `:root`：

```css
:root {
  --primary: #2F6DB0;   /* 应援蓝·主色 */
  --accent: #D8546A;    /* 喜爱红·点缀色 */
  --nav-h: 68px;        /* 导航栏高度，各页顶部留白依赖它 */
  --maxw: 1140px;
}
```

### 测试改动

- **最快**：双击 `index.html` 用浏览器打开（数据是 JS，`file://` 下能正常渲染）
- **部署**：`git add . && git commit -m "..." && git push`

### 自动化自检（改完务必跑一次）

**两个脚本分工不同，建议都跑：**

**① 渲染层** —— 容器有没有被填充、条目数对不对

```bash
cd ju-jingyi-fansite
NODE_PATH="C:/Users/27378/.workbuddy/binaries/node/workspace/node_modules" \
  "C:/Users/27378/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" \
  .workbuddy/e2e-render-test.js
```

用 jsdom 渲染全部 6 个页面（含详情页 3 种参数），共 **167 条断言**，覆盖：
外壳渲染、各板块条目数、脚本顺序、**`.reveal` 元素是否全部被观察**（防 3.4 那个坑回归）、
**跨页面比对**（首页里程碑 vs 关于她成长历程、首页动态 vs 动态页文章，是否逐字一致）、
以及**全站 head 检查**（六个页面的 `og:` 标签、favicon、theme-color 是否齐全）。

**② 数据层** —— 同一个事实在多个文件里是否一致、字段是否完整（不依赖 jsdom）

```bash
"C:/Users/27378/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" \
  .workbuddy/data-consistency-test.js
```

检查 7 组：单一源头不变量、首页作品卡年份与作品页一致、
**里程碑年份被星河纪事覆盖（含 timeline `date` 格式校验）**、
关键事实多点引用一致（出道年份 / 应援色 / 粉丝名 / 更名时间 / 合约表述 / 旧站名残留）、
作品字段完整性、文章结构完整性、各文件键完整性。

> **改了数据条数必须同步更新 `e2e-render-test.js` 里的期望值**，否则误报。
>
> ⚠️ 另外两条容易踩的同步点：
> 1. `data-consistency-test.js` 按 `date.startsWith('2024')` 这类**年份前缀**匹配，
>    所以 timeline 的 `date` 必须以 4 位年份开头（脚本已加断言校验）。
> 2. 若某一年出现**多条** timeline 条目，不要用 `find(...)` 取单条做事实检查 ——
>    取该年**全部条目**合并后再检查（2024 年有 3 条，合约只是其中之一）。
> 两个脚本的依赖情况：`e2e` 需要 `jsdom`（装在 WorkBuddy 的 node workspace 里，不在本仓库内，保持仓库零依赖）；
> `data-consistency` 只用 Node 内置模块，可直接跑。

---

## 六、技术约束与注意事项

### 必须遵守

1. **数据文件必须是 JS**，不能用 `.txt` / `.md` / `.json`。
   原因：浏览器在 `file://` 下禁止 JS 读取本地 txt/md/json（CORS），会导致本地双击打不开。
2. **数据字段名不要随意改**，渲染器按字段名取值（见第四节各表的「标签字段」列）。
3. **渲染顺序**：`data/*.js` 在 `main.js` 之前；`main.js` 内部顺序见 3.4。
4. **新增内容不要写进 HTML**，写进 `data/`。

### 已知取舍

- **站点强依赖 JavaScript**：导航、页脚、全部内容都由 JS 渲染，禁用 JS 会看不到正文。
  这是「零依赖 + 数据驱动 + 本地双击可看」三者兼得的代价，已接受。
  好处是换域名不用改任何代码（站内全是相对路径）。
  **缓解措施**：6 个页面都有 `<noscript>` 兜底（站名 + 说明 + 五个页面链接 + 合规声明），
  自检有断言盯着。⚠️ 但这只解决「白屏没解释」——
  **搜索引擎爬虫不执行 JS，所以站内作品/文章内容基本搜不到**；
  分享卡片不受影响，因为 `og:` 标签是静态写在 head 里的。
- **ICP 备案号待展示**：备案通过后，工信部要求网站底部展示备案号并链到 `beian.miit.gov.cn`。
  目前页脚渲染器（`main.js` 外壳段的 `<div class="bar">`）**没有备案号的槽位**，`footerGroups` 也只支持链接组。
  到时候要做两件事：① 在 `site.js` 的 `meta` 里加一个 `icp` 字段；
  ② 在页脚渲染器里把它渲染成指向 `https://beian.miit.gov.cn/` 的外链。
  （临时应付也可以直接写进 `meta.copyright`，但那样不可点击，不合规范建议。）
  自检的 `data-consistency-test.js` 里有 `SITE` 键完整性检查，加字段后要同步那份键列表。
- **详情页网址带参数**：`update.html?id=xxx`。换来的是「加动态只改数据、不新建文件」。
  站点尚未公开，不用担心旧链接失效。

### 已知设计约定

- **配色**：蓝（应援色）为主 + 红（喜爱色）点缀。
- **响应式断点**：`900px`（三列→两列）、`680px`（单列 + 汉堡菜单，并隐藏导航英文副标题）。
- **无障碍**：导航按钮用 `<button>` + `aria-*`；图片有 `alt`；灯箱支持 Esc 关闭。
- **动效**：`.reveal` 做滚动淡入，`prefers-reduced-motion` 下自动禁用。

---

## 七、合规红线（重要）

本站是**非官方**粉丝站，必须保持：

1. 页面均有**非官方声明**（与艺人及经纪公司无关）
2. 图片标注**图源**（微博 @鞠婧祎）
3. 明确**禁止商用**
4. 内容须**可溯源**（资料参考百度百科等公开来源）
5. 经纪合约等**有争议的信息**须**中立表述**（如「本人方宣布不续约，公司随后提出异议」）

**加内容时请延续这个口径，不要写成官方口吻，不要收录无法核实的信息。**

---

## 八、当前状态

**已完成（1.0 结构段）**

- 内容全部外置到 `data/`，HTML 里只剩空容器
- 导航 / 页脚 / 合规声明抽为全站共享外壳，消灭 7 份副本与文案漂移
- **确立「共用内容单一源头」规则**（见 3.6）：
  成长里程碑 → `SITE.milestones`（关于她取全部 9 条、首页取前 4 条）；
  近期动态 → `NEWS.articles`（动态页取全部、首页取前 2 条）
- 各页区块标题、首页全部残留内容均已数据化
- 详情页改为数据驱动（`update.html?id=`），旧的 `update-magazine.html` / `update-tutumian.html` 已删除
- 统一页头间距（原关于页 80px / 其余 70px）、统一页脚版权行文案
- 补上 `works.html` / `news.html` 缺失的非官方声明
- 修复「滚动揭示动画早于数据渲染导致卡片永久透明」的顺序 bug
- 新增两个自检脚本：渲染层 **167 条断言** + 数据层 7 组一致性检查，均全绿

**已完成（1.0 内容段 · 关于她）**

- 三路子代理交叉核实，已在 `about.js` 顶部注释记录证伪结论（白玉兰提名、万事如祎工作室）
- 简介 3 段、应援信息 3 张、荣誉 11 张
- 成长历程按「里程碑」标准精编为 9 条

**已完成（1.0 内容段 · 作品）**

- 三路子代理交叉核实（音乐 / 影视 / 综艺三条线分别独立检索）
- 已修正：《热血长安》2016（拍摄年）→ **2017**（播出年）；
  《如意芳霏》的《梦渡》由「主题曲」→ **「片尾曲」**
- 已补漏：OST 由 11 首补到 **18 首**（新增《麻辣变形计》主题曲《Fighting Day》、
  《魔天劫》主题曲《缘尽世间》、《风暴舞》插曲《倒流》、《茶啊二中》推广曲《那一天》、
  《恋与制作人》片尾曲《梦的旅航》、《天官赐福》天下清、《青云传》主题曲《天长》）；
  单曲新增《琢光曲》《今晚月色真美》《灯火流光》《Rosa》；
  **综艺由 3 条扩到 12 条**
- 已标注翻唱：《花》《壁上观》，以及《新白娘子传奇》三首经典曲目
- 已补全一人多角：《花戎》3 角（魏枝 / 司马忘月 / **魔后阿崖**）、《花间令》2 角（杨采薇 / 上官芷）、
  《月鳞绮纪》2 角（露芜衣 / 地珠）、《千香》加「小棒槌」
- 板块小标题改为「影视 · 动画 · 游戏原声」（原「影视原声」装不下手游 / 动画歌曲）
- works.html 补上独立的页面大标题（原来「音乐作品」区块头兼作页面标题，与其他页结构不一致）

**已完成（1.0 内容段 · 动态）**

- 三路子代理交叉核实（作品播出线 / 活动公益线 / 团体与合约线）
- 星河纪事由 **14 条（每年 1 条）扩到 37 条**，逐年铺开并补上精确日期
  → ⚠️ **随后按站主决定从动态页移除**（理由：历年记录与「动态」栏目语义不符）。
     动态页改为只保留「近期动态」；数据保留在 `NEWS.timeline` 但不再展示（见 3.6）
- **新收录此前完全空缺的社会活动与公益**：2018 公益 MV《未来已来》、2021 河南暴雨捐款、
  2021 联合品牌向中国妇女发展基金会捐赠
- 补齐晚会演出：央视元宵 / 央视春晚 / 湖南卫视五四 / 北京卫视春晚 / 河南卫视国潮盛典与中秋奇妙游 /
  浙江卫视跨年 / 江苏卫视春晚与元宵 / 快手超级夏晚 / QQ 音乐超级巅峰之夜
- 补齐团体时期细节：各届总决选名次与票数、2017-12-15 晋升明星殿堂、2018-03-04 剧场告别演出
- 合约节点按「本人方说法 / 公司方说法」分列，**未作判断**
- 自检同步：`date` 改为带月日格式后，一致性脚本的年份匹配改为**前缀匹配**并加了格式断言

**已完成（1.0 内容段 · 首页 + 分享优化）**

- 首页内容**核实完毕**：作品卡 6 个条目年份与作品页一致（有一致性断言盯着）、
  摘要与应援档案文案均准确 → 无需修改
- 补上**社交分享标签**：6 个页面各 7 个 `og:` 标签 + `twitter:card` ——
  分享到微信 / 微博 / QQ 时卡片不再只有光秃秃的标题
- 补上 **favicon**（SVG 为主 + PNG 兜底 + `apple-touch-icon`）与 `theme-color`
- 生成专用分享封面 `assets/img/og-cover.jpg`（1200×918、72KB，由 `gallery-01.jpg` 压到约 1/10）
  → **刻意没有裁成 1.91:1**：那样会切掉右下角的微博水印，而站点有「图源标注」的合规要求
- 自检新增 18 条 head 断言（每页：og 标签齐全 / 有 favicon / 有 theme-color）

**已完成（2026-09-17 全站五路交叉复核）**

用 5 个子代理按信息域并行复核（人物生平 / 音乐 / 影视 / 综艺荣誉 / 时效性）。
**结论：绝大部分数据准确** —— 9 条里程碑、16 部影视、12 条综艺、10 条荣誉基本无误。

修正 4 处：

- 《过去完成时》删除「曾被伯克利音乐学院学生翻唱」—— **两轮检索均无来源**
- 《缘尽世间》归属改为 **手游《魔天记》主题曲**（原误写为微电影《魔天劫》主题曲，二者为同一 IP）
- 《梦的旅航》补上合作者（与许佳琪、孔肖吟合唱）
- 综艺末条「2020 起」→「**2019 起**」（她 2019-02-19 已登上央视元宵晚会）

补 2 首遗漏：**《FIND THE WAY》**（2026-06-18 生日单曲，四源确认）、**《平凡与伟大》**（2020 与霍尊合唱）

> ⚠️ **代理结论冲突一例**：某代理报称《来战》「已定档 2026 年 9 月」，
> 另一代理与逐条检索**均无来源支撑**（媒体口径只有"预计 2026 下半年 / 暂定 Q3"，无官方定档官宣）
> → 站内「已杀青 · 待定档」**正确，未改**。
> **这说明代理之间的交叉验证是必要的，单个代理的"发现"不能直接采信。**

**已完成（2026-09-17 站主反馈落实）**

- 合约表述按站主决定**维持现状** —— 以「到期不续约」为主述，不加"以司法结论为准"那句
- 增补荣誉 1 项：**微博之夜「年度进取艺人」**（2018、2021 两度）→ 荣誉 10 → **11 条**
- 动态页补 3 篇，由 2 → **5 篇**：FRED 斐登中国区品牌大使（2026.07）、
  32 岁生日单曲《FIND THE WAY》（2026.06.18）、帕莎眼镜全球品牌代言人（2026.01）
  → 另给 PORT 那篇补上「创刊以来首位女性封面人物」
- 《如意芳霏》三曲定位**核实后维持不变**：五个来源中 4 个支持站内写法（《梦渡》＝片尾曲）

> ⚠️ **量级要分清**：FRED 是「**中国区品牌大使**」，不是「全球代言人」——
> 与凸凸棉（全球代言人）、帕莎（全球品牌代言人）不是一个档位，数据里已分别写明。

**已完成（2026-09-17 · 动态页增开「品牌合作」板块）**

按站主要求：「9 个商务代言全部纳入，但每条要有官宣链接。」

- 动态页新增 **`NEWS.brands`（10 条）** 与独立板块，每条附**品牌方官方微博的官宣帖链接**
- ⚠️ 头衔**逐条逐字核对**过，分四种：全球品牌代言人 / 品牌全球代言人 /
  超级会员代言人 / **服饰品牌大使**（淘宝×天猫是"大使"不是"代言人"）
- 未写成 10 篇文章 —— 那会把动态页变成广告墙；**列表形式更适合"一览"这个用途**
- 同步补 3 首 SNH48 公演曲个人版进 `albums`（《冬日》《爱未央》《以十洲的名义》）→ 18 → **21**
- 《缘尽世间》补上合作者（与赵嘉敏、许佳琪合作）

**已完成（2026-09-17 · 图库扩充 9 → 59 张）**

站主提供 50 张原图（桌面 `50图` 文件夹）。处理过程：

- 全部为 **JPEG，且扩展名与真实格式一致**（这次没踩格式坑）
- ⚠️ 但原始体积 **112.6 MB**（单张最大 6.6MB、最长边 8000px），直接接入会让页面打不开
- 用 Pillow 统一压到「**长边 1600px + 质量 82 + 渐进式**」→ **9.0 MB（降到 8%）**，画质肉眼无差
- **原图一个都没动**（只读桌面文件，输出到 `assets/img/gallery-10.jpg` ~ `gallery-59.jpg`）
- 渲染器加 `loading="lazy"` + `decoding="async"` —— 59 张不再一次性全下载
- 标签由「营业图 · 一~九」统一为「**图库 · 1~59**」（与页面标题一致）；
  `pageHead.title` 从「营业图」改为「**图库**」（与导航项一致）；desc 中"营业图"改"公开图片"
  （内容含少量综艺节目照，不全是营业写真）
- 内容构成：以写真 / 棚拍为主，另含少量综艺节目照与直播画面、几组同造型连拍

**待办**

- 图库可继续补充 —— **新图请先压到 300KB 以内**再放进 `assets/img/`
- 动态页 `articles` 现有 5 篇，可继续补充（真实的杂志 / 品牌 / 活动素材）
- `og:image` 目前是相对路径 —— **绑定自定义域名后改成绝对地址**
  （六个页面的 head 里都留了 ⚠️ 注释标明这一点）
- 国内节点需绑定**已 ICP 备案的自定义域名**才能公开访问（海外 `.edgeone.dev` 正常，但微信内 401）
